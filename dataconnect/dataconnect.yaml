specVersion: "v1"
serviceId: "leave-management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "leave-management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
